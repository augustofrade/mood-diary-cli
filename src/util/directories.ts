import path from "path";

export const basePath = path.join(__dirname, "../../diary");
export const configPath = path.join(basePath, "config.json");
export const jsonPath = path.join(basePath, "entries");